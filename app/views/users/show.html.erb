<h1 class="welcome">Welcome, <%= @user.username %></h1>

<% if !@user.beers.empty? %>
    <%= render partial: 'usernav', locals: {user: @user} %>
<% else %>
    <h2>Get Started:</h2>
    <div class="nav-grid">
        <div>
            <%= link_to image_tag("new-beer.jfif", class: "new-beer"), new_beer_path %><br>
            Add a New Beer
        </div>
    </div>
<% end %>

<%= render partial: "welcome/nav" %>

