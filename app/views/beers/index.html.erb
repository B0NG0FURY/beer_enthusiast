<% if @user %>
    <h1>Beers <%= @user.username %> has tried</h1>
<% else %>
    <h1>Beers</h1>
<% end %>

<div class="info-list">
    <% sort_beers_by_name(@beers).each do |beer| %>
            <%= render partial: "beer", locals: {beer: beer} %>
    <% end %>
</div>