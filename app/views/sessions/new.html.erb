<h1>Login</h1>

<%= render partial: "flash" %>

<%= form_for @user, url: '/login' do |f| %>
    <%= render partial: 'users/form', locals: {f: f} %>
    <%= f.submit "Login" %>
<% end %>
<br>
<%= link_to "Sign in via Google", '/auth/google_oauth2', method: :post %>