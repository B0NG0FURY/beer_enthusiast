<h2>Create A Review For <%= @beer.name %></h2>

<%= form_for @review do |f| %>
    <%= f.hidden_field :user_id, value: @user.id %>
    <%= f.hidden_field :beer_id, value: @beer.id %>

    <%= render partial: 'form', locals: {f: f} %>
    
    <%= f.submit "Add Review" %>
<% end %>