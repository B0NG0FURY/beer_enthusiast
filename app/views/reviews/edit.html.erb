<h1>Edit Review for <%= @review.beer.name %></h1>

<%= form_for @review do |f| %>
    <%= render partial: 'form', locals: {f: f} %>

    <%= f.submit "Edit Review" %>
<% end %> 
<br>
<%= button_to "Delete Review", review_path(@review), method: :delete, data: { confirm: "Delete Review?" } %>
