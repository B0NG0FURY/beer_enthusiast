<h1>Edit Review for <%= @review.beer.name %></h1>

<%= form_for @review do |f| %>
    <%= f.label :rating, "Rating (0-100)" %>
    <%= f.number_field :rating, min: 0, max: 100 %><br>

    <%= f.label :comment %>
    <%= f.text_area :comment %><br>

    <%= f.submit "Edit Review" %>
<% end %>